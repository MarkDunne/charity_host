<% content_for :page_title do %>
	Configure Appearance
<% end %>

<h3>Change text color</h3>
<%= form_for @charity.settings, url: appearance_path(@charity.id, :update_template), html:{class: "form-horizontal", role: "form"} do |f| %>

  <div class="form-group">
    <%= f.label :text_color, "Text Color", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :text_color, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Save Changes", :class => "btn btn-default" %>
    </div>
  </div>
<% end %>